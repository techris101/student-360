name: Deadline Reminders

on:
  schedule:
    # 07:00 Kigali time (UTC+2) -> 05:00 UTC daily
    - cron: "0 5 * * *"
  workflow_dispatch:

jobs:
  reminders:
    name: Check Deadlines & Send Reminders
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Enable Corepack & Setup pnpm
        run: |
          corepack enable
          corepack prepare pnpm@12.5.1 --activate

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Deadline Reminders Check
        run: pnpm exec tsx scripts/pipeline/reminders.ts
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          NEXT_PUBLIC_SITE_URL: https://student-360.vercel.app
